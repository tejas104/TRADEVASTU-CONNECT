{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run db:push:prod && npm run build:server"
  },
  "deploy": {
    "startCommand": "npm run start"
  },
  "nixpacks": {
    "setup": {
      "nixPkgs": ["nodejs", "npm"]
    }
  }
}
